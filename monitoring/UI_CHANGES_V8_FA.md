# تغییرات ظاهری و Live Data — v8

- دکمه Grafana از بخش Finance حذف شد.
- باکس منفرد `Reddit realtime health` از Dashboard حذف شد.
- باکس توضیحی `خروجی‌های Native Pipeline` از صفحه Data حذف شد.
- صفحه Data اکنون Reddit، YouTube و X را هر ۲ ثانیه refresh می‌کند.
- YouTube از `youtube_comments_v2.jsonl` به‌صورت read-only خوانده می‌شود.
- X از جدول `tweets_raw` در `twitter_data_v4.db` به‌صورت read-only خوانده می‌شود.
- هیچ فایل داخل `repo/` برای این تغییرات ویرایش نشده است.
