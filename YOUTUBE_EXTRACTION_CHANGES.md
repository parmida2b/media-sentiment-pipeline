# تغییرات pipeline استخراج YouTube — توضیحات کامل

این فایل توضیح می‌دهد چه چیزی در `extraction/youtube_extract.py` و فایل‌های کنارش عوض شده، چرا این تغییرات لازم بود، دیتاهای جدید چه شکلی هستند، اسکریپت را چند بار و چطور باید اجرا کنید تا داده‌ها کامل جمع شوند، و بعد از استخراج چه باید کرد.

## ۱. قبلاً چه بود؟

اسکریپت قدیمی خیلی محدود بود:
- فقط ۴ query عمومی جستجو می‌کرد و فقط ۲ کانال هاردکد شده داشت: CNN و NYT.
- هیچ تنوع جغرافیایی/دیدگاهی در منابع نبود — عملاً بیشتر نتایج از رسانه‌های غربی/آمریکایی می‌آمد.
- هیچ فیلتری برای اینکه ویدیو واقعاً به جنگ ایران-آمریکا مربوط هست یا نه وجود نداشت — کامنت هر ویدیویی که پیدا می‌شد، بدون بررسی گرفته می‌شد.
- هیچ اطلاعاتی درباره اینکه یک ویدیو از چه کشور/دیدگاهی است ذخیره نمی‌شد.
- اسکریپت یک‌بار مصرف بود: اگر وسط کار quota روزانه API تمام می‌شد، کل پیشرفت از دست می‌رفت و باید از اول اجرا می‌شد.

## ۲. الان چه چیزی عوض شده و چرا

### الف) تنوع منابع بر اساس کشور و دیدگاه (`extraction/channels.py` — فایل جدید)
یک رجیستری ساختاریافته از ۱۸ کانال در ۶ دسته اضافه شد:
`iran_state`, `iran_diaspora`, `us_western`, `arab_gulf`, `european_western`, `international_thinktank`.

هر کانال با `customUrl` (handle) واقعیش تأیید می‌شود، نه با اعتماد به ترتیب نتایج جستجو — یعنی اگر جستجو یک کانال اشتباه با اسم مشابه برگرداند، سیستم آن را رد می‌کند و فقط warning می‌دهد.

**چرا:** بدون این تنوع، تحلیل نهایی نمی‌تواند احساسات را بین رسانه‌های ایرانی/غربی/عربی/اروپایی مقایسه کند — که دقیقاً هدف اصلی پروژه است.

### ب) تنوع جغرافیایی از طریق regionCode
علاوه بر انتخاب دستی کانال، همان queryهای عمومی حالا روی ۶ منطقه مختلف (`IR, US, GB, DE, FR, AE`) با زبان مرتبط هرکدام (`relevanceLanguage`) هم اجرا می‌شوند.

**چرا:** تنوع منابع را از وابستگی کامل به انتخاب دستی کانال‌ها خارج می‌کند — یوتیوب بر اساس منطقه، نتایج متفاوتی نشان می‌دهد.

### ج) برچسب‌گذاری جغرافیایی/دیدگاهی با LLM + فیلتر ارتباط (`extraction/geo_tagger.py` — فایل جدید)
برای هر ویدیو (نه هر کامنت) یک‌بار از مدل **Groq (llama-3.3-70b-versatile)** خواسته می‌شود این‌ها را تشخیص دهد:
- `origin_country` (کشور منبع)
- `perspective` (state_media / western / independent / diaspora / other)
- `is_relevant` (آیا واقعاً به جنگ ایران-آمریکا مربوط است؟)
- `confidence`

اگر ویدیو `is_relevant=false` باشد، کامنت‌هایش اصلاً گرفته نمی‌شود.

نتیجه این تگ‌گذاری برای هر video_id **cache** می‌شود — یعنی برای ویدیویی با ۵۰۰ کامنت، فقط یک‌بار LLM صدا زده می‌شود، نه ۵۰۰ بار.

**چرا:** هم quota و زمان روی ویدیوهای بی‌ربط هدر نمی‌رود، هم امکان مقایسه احساسات بین دیدگاه‌های مختلف رسانه‌ای فراهم می‌شود.

**نکته مهم درباره fail-open:** اگر هیچ‌کدام از `GEMINI_API_KEY`/`GROQ_API_KEY` تنظیم نباشد، این ماژول کرش نمی‌کند بلکه «fail-open» می‌رود: همه‌ی ویدیوها را بدون بررسی واقعی `is_relevant=true` و `perspective="other"` در نظر می‌گیرد (`confidence=0.0` تا مشخص باشد این رکورد واقعاً توسط LLM تحلیل نشده). این حالت یک‌بار در ابتدای این فاز اتفاق افتاد و باعث شد حدود ۱۰۶ ویدیو بدون فیلتر واقعی تگ بخورند؛ چون این تگ‌ها cache می‌شوند، فقط وصل کردن Groq بعداً کافی نبود — لازم شد `checkpoint.json` و `video_geo_metadata.jsonl` ریست شوند تا آن ویدیوها دوباره با LLM واقعی بررسی شوند.

### د) فایل جانبی متادیتا (`data/raw/video_geo_metadata.jsonl` — فایل داده جدید)
نتیجه‌ی تگ‌گذاری هر ویدیو در این فایل ذخیره می‌شود، **بدون دست‌زدن به `config/schema.py`** (طبق قرارداد تیمی که آن فایل فقط با حسین هماهنگ می‌شود).

**چرا:** بعداً با join کردن این فایل و فایل کامنت‌ها از طریق `video_id`/`post_id`، می‌توان تحلیل منطقه‌ای/دیدگاهی انجام داد، بدون اینکه فرمت مشترک داده تیم به‌هم بریزد.

### ه) مدیریت quota و قابلیت resume (`extraction/checkpoint.py` — فایل جدید)
YouTube Data API روزی فقط ۸۰۰۰-۱۰۰۰۰ واحد سهمیه رایگان می‌دهد و هر جستجو (`search.list`) ۱۰۰ واحد مصرف می‌کند — با این حجم کانال و منطقه، این سهمیه به‌راحتی تمام می‌شود. برای همین یک لایه checkpoint اضافه شد که:
- یادش می‌ماند کدام ترکیب کانال/query/region قبلاً جستجو شده (`data/raw/checkpoint.json`).
- یادش می‌ماند کدام ویدیو کامنتش گرفته شده یا برچسب خورده.
- وقتی سهمیه امروز تمام شود، اسکریپت به‌آرامی متوقف می‌شود (نه کرش) و پیام می‌دهد که باید بعداً دوباره اجرا شود.
- اجرای بعدی دقیقاً از همان‌جایی که متوقف شده ادامه می‌دهد — هیچ جستجو یا فچ تکراری انجام نمی‌شود.

**چرا:** بدون این، هر بار که quota تمام می‌شد، کل کار از اول شروع می‌شد و هیچ‌وقت به تنوع کامل منابع نمی‌رسیدیم.

**نکته Windows/OneDrive:** ذخیره‌ی checkpoint با نوشتن یک فایل موقت (`checkpoint.json.tmp`) و جایگزین کردن اتمیک آن با `os.replace` انجام می‌شود. چون پروژه داخل پوشه‌ی OneDrive قرار دارد، گاهی OneDrive (یا آنتی‌ویروس) لحظه‌ای روی فایل قفل موقت می‌گذارد و `os.replace` با خطای `PermissionError: WinError 5` مواجه می‌شود. برای همین `save_checkpoint` حالا تا ۵ بار با backoff افزایشی (۰.۲ تا ۱ ثانیه) دوباره تلاش می‌کند قبل از اینکه واقعاً خطا بدهد.

### و) لینک درخواستی شما
ویدیوی `https://www.youtube.com/watch?v=bQI8B93CnJg` مستقیماً به لیست ویدیوهای ثابت (`EXPLICIT_VIDEO_IDS`) اضافه شد — بدون صرف quota جستجو، همیشه پردازش می‌شود.

## ۳. چه دیتاهای جدیدی تولید می‌شود؟

| فایل | چیست | برای تحلیل نهایی لازم است؟ |
|---|---|---|
| `data/raw/youtube_comments_<start>_to_<end>.jsonl` | همان فرمت قبلی کامنت‌ها (طبق `schema.py`) — فقط حالا از منابع خیلی متنوع‌تر و فقط برای ویدیوهای مرتبط | بله، داده اصلی |
| `data/raw/video_geo_metadata.jsonl` | یک رکورد به‌ازای هر ویدیو: `video_id, source_channel, source_country, region, perspective, language, is_relevant, confidence` | بله — برای join کردن با کامنت‌ها و تفکیک منطقه‌ای/دیدگاهی |
| `data/raw/checkpoint.json` | فقط state داخلی اجرای اسکریپت (چی جستجو شده، چقدر quota مصرف شده) | خیر — فقط برای resume، ربطی به تحلیل ندارد |
| `data/raw/resolved_channels.json` | کش channel_id هر کانال که یک‌بار resolve شده | خیر — فقط cache داخلی |

**نکته مهم:** چون اسم فایل کامنت‌ها بر اساس تاریخ «تا امروز» ساخته می‌شود، اگر اسکریپت را در چند روز مختلف اجرا کنید، چند فایل `youtube_comments_*.jsonl` جدا خواهید داشت. برای تحلیل باید **همه** این فایل‌ها را با هم بخوانید (glob کنید)، نه فقط آخرین فایل را.

## ۴. چند بار و چطور باید اجرا شود؟

دستور اجرا همیشه همین است:
```bash
python extraction/youtube_extract.py
```

با تعداد فعلی کانال‌ها و query/region، کل مرحله‌ی discovery (پیدا کردن ویدیوها) حدود **۴۲۰۰ واحد quota** لازم دارد که زیر سقف روزانه (۸۰۰۰ واحد پیش‌فرض، در `.env` قابل تغییر با `YOUTUBE_DAILY_QUOTA_BUDGET`) است — یعنی معمولاً discovery در **همان اجرای اول** کامل می‌شود. فچ کامنت و تگ‌گذاری بعد از آن خیلی ارزان‌تر است (۱ واحد به‌ازای هر ویدیو تقریباً)، پس اجرای اول معمولاً همه‌چیز را هم کشف و هم دانلود می‌کند.

اما اگر:
- تعداد کانال‌ها/queryها بعداً زیاد شود،
- یا تعداد ویدیوی کشف‌شده خیلی زیاد باشد،

ممکن است در انتهای خروجی این پیام را ببینید:
```
[quota] Daily budget reached. Progress saved — re-run later to continue.
```
در این حالت:
1. صبر کنید تا سهمیه ریست شود (نیمه‌شب به وقت Pacific Time، یعنی تقریباً ۱۰:۳۰-۱۱:۳۰ صبح روز بعد به وقت ایران، بسته به فصل).
2. دقیقاً همان دستور را دوباره اجرا کنید — از همان‌جا ادامه می‌دهد.
3. این کار را تکرار کنید تا دیگر پیام quota تمام‌شده را نبینید و تعداد ویدیوهای discovered با comments_fetched برابر شود.

خلاصه: **معمولاً یک اجرا کافی است**، ولی برای اطمینان کامل، اسکریپت را ۲-۳ روز پشت‌سر‌هم اجرا کنید تا مطمئن شوید چیزی جا نمانده — خصوصاً اگر بعداً لیست کانال‌ها را در `channels.py` گسترش دادید.

## ۵. مرحله بعد از استخراج داده چیست؟

استخراج فقط داده خام تولید می‌کند؛ برای رسیدن به خروجی نهایی (مقایسه احساسات بین گروه‌ها، روند زمانی، Power BI) باید:

1. **Join کردن دو فایل:** `video_geo_metadata.jsonl` را بر اساس `video_id` با `youtube_comments_*.jsonl` بر اساس `post_id` ترکیب کنید تا هر کامنت برچسب `region`/`perspective`/`source_country` بگیرد.
2. **اجرای تحلیل احساسات واقعی روی کل داده:** فایل فعلی `sentiment/compare_llm_sentiment.py` فقط یک تست کوچک مقایسه‌ای (Day 1 scouting) روی ۲۰ نمونه است، نه pipeline نهایی — باید یک نسخه batch از آن نوشته شود که روی همه رکوردهای واقعی (بعد از join) اجرا شود و برچسب sentiment را برای هر کامنت ذخیره کند.
3. **تجمیع آماری زبان کامنت‌ها (لایه‌ای که در این تغییر پیاده نشد، فاز بعدی است):** طبق برنامه اولیه ، باید یک اسکریپت جدا (مثلاً `sentiment/comment_language_stats.py`) درصد کامنت‌های فارسی/عربی/انگلیسی/فرانسوی/آلمانی را به‌عنوان proxy جغرافیا محاسبه کند — نه location فردی افراد. این را عمداً به بعد از جمع‌آوری چند روز داده واقعی موکول کردیم چون تعریف «درصد» بدون داده واقعی معنی‌دار نیست.
4. **ترکیب با بقیه تیم:** داده نهایی باید با استخراج Reddit (حسین)، داده مالی/اقتصادی (علی) و ارزیابی جامعه آماری/داشبورد (ریحانه) هماهنگ و merge شود — طبق تقسیم‌کار در `README.md`.
5. **بارگذاری در Power BI:** بعد از join و تحلیل sentiment، خروجی نهایی (با فیلدهای region/perspective/sentiment/تاریخ) آماده‌ی ساخت نمودار مقایسه‌ای و heatmap/map در Power BI است.

## ۶. نکات دیگری که لازم است بدانید

- **Handleهای کانال حدسی‌اند:** چند تا از handleها (مثلاً `presstv`, `irinnnews`, `voapersian`) بر اساس حدس منطقی انتخاب شدند و تأیید نشده‌اند. اولین اجرای اسکریپت اگر handle اشتباه باشد، پیام `[warn] could not confirm official channel` می‌دهد — در آن صورت handle درست را در `extraction/channels.py` اصلاح کنید.
- **quota بین کل تیم مشترک است:** اگر چند نفر از یک `YOUTUBE_API_KEY` مشترک استفاده می‌کنند، سهمیه روزانه بین همه اجراها (نه فقط اجراهای شما) تقسیم می‌شود — بهتر است هماهنگ کنید کی چه زمانی اسکریپت را اجرا می‌کند تا quota همدیگر را نگیرید.
- **فایل‌های state لوکال و per-machine هستند:** `checkpoint.json` و `resolved_channels.json` طبق `.gitignore` فعلی commit نمی‌شوند. یعنی اگر امروز شما اجرا کنید و فردا هم‌تیمی دیگری روی سیستم خودش اجرا کند، آن‌ها checkpoint شما را نمی‌بینند و از اول شروع می‌کنند (و quota مشترک را زودتر مصرف می‌کنند). اگر قرار است چند روز پشت‌سرهم ادامه‌دار اجرا شود، بهتر است همیشه از یک سیستم/نفر مشخص اجرا شود، یا این فایل‌ها دستی بین اعضا رد و بدل شوند.
- **`.env.example` هنوز کلید واقعی دارد:** این فایل به‌جای placeholder، مقدار واقعی `YOUTUBE_API_KEY` و `GROQ_API_KEY` دارد. خوشبختانه هیچ‌وقت commit نشده، ولی پیشنهاد می‌شود جدا از این تغییرات با placeholder جایگزین شود تا ریسک لو رفتن تصادفی نداشته باشد.
